# 开发执行看板

## 使用方式

本文件用于维持开发上下文一致性。每一轮开发开始前确认目标，结束后更新完成情况、验证结果和下一步。

## 当前阶段

阶段 10：多轮上下文 Memory。

## 本轮目标

- 新增短期 Agent Memory。
- 支持多轮上下文引用和候选纠正。
- 补充 Memory 单元测试和浏览器回归。

## 已完成

- 题目分析和评审规则梳理。
- 项目 README。
- 文档索引。
- 开发计划、资源清单、技术选型。
- 前端、后端、测试、运维设计。
- 风险预案、验收清单、演示脚本。
- React + Vite + TypeScript 项目骨架。
- 语音日历工作台首屏占位。
- 基础目录结构和构建脚本。
- 日历事件数据模型。
- localStorage 事件存储。
- 事件列表接入真实本地状态。
- 填充演示数据、清空数据、删除事件。
- 文字指令 Agent 初版。
- 新增、查询、修改、删除意图解析。
- 写操作 `pendingAction` 确认卡片。
- Agent 解析器单元测试。
- 浏览器 Web Speech API 封装。
- 语音识别结果接入文字指令 Agent。
- SpeechSynthesis 语音播报。
- 语音不可用时保留文字输入。
- 最近一次操作撤销。
- 新增事件冲突检测。
- 本地事件 JSON 导出。
- 空闲时间查询。
- 修复删除 / 修改候选匹配的时间基准问题。
- 补充空闲时间查询单元测试。
- 前端视觉优化和移动端布局修复。
- Agent 与语音能力升级计划文档。
- FastAPI 后端骨架。
- DeepSeek `/api/parse-command` 代理接口。
- 前端在本地规则 `unknown` 时调用后端 LLM 兜底。
- `faster-whisper` `/api/asr` 本地语音识别接口。
- ASR 并发保护，默认 `ASR_MAX_CONCURRENT=1`。
- ASR CPU/GPU 环境变量开关。
- 后端自动读取 `.env.local` / `.env`。
- 后端健康检查返回 DeepSeek 与 ASR 配置状态。
- 前端麦克风已切换到本地 ASR 路径。
- 删除 / 修改命中多个候选时先选择候选，不再默认第一个。
- 支持通过“第一个 / 第二个”等文字或语音选择候选。
- 支持“它 / 那个 / 刚才那个”引用最近提到的日程。
- 支持对最近引用日程执行“提前十分钟提醒”类提醒修改。
- 扩展本地规则解析：一刻、半点、明确结束时间、下周日期范围、下周工作日。
- 扩展本地规则测试到 8 个用例。
- 增加月历视图，支持切换月份、回到今天、点击日期查看当天日程。
- 技术含量提升路线图文档。
- Agent 状态机基础抽取：新增 `agentStateMachine.ts`，集中管理消息、待确认动作、撤销动作和最近引用事件。
- Agent 状态机测试：覆盖多候选选择、选择候选进入确认、取消清空待处理动作。
- Agent 待确认动作 factory 抽取：新增/删除/修改/提醒的 pending action 统一由状态机模块创建。
- Agent 确认执行结果抽取：状态机模块输出 create/delete/update/needs_selection 纯结果，UI 只负责执行日历副作用。
- 解析器评测集初版：新增 9 条数据驱动用例，覆盖新增、查询、删除、修改、空闲时间、下周范围和缺时间降级。
- 修复查询意图过宽问题：`帮我安排一个复盘` 不再被误判为查询日程。
- 浏览器端完整演示路径回归：新增、查询、无日期修改、冲突提示、删除二次确认、撤销恢复均通过。
- 修复无日期修改的日期继承问题：`把产品评审会改到下午四点` 会沿用匹配日程原日期。
- 修复 `下周日` 解析边界：统一按下一周周一为基准计算下周星期。
- 解析器评测集扩展到 19 条数据驱动用例，覆盖提醒式创建、口语约会、无日期删除、显式结束时间修改、下周日、空查询和未知输入。
- 演示脚本补充可直接照读的讲稿和已回归路径。
- 新增部署说明文档 `docs/19-部署说明.md`，覆盖静态部署、可选后端、环境变量和上线检查。
- 解析器评测集扩展到 55 条中文口语样例，覆盖创建、查询、删除、修改、空闲时间、未知输入和安全候选。
- 增加解析器聚合指标输出：intent accuracy、slot accuracy、fallback rate、unsafe action rate。
- 新增 `npm run eval:parser`，可单独运行解析器评测集并输出质量指标。
- 收紧删除 / 修改安全匹配：带标题但未命中候选时不再退化成日期范围内任意日程。
- 扩展时间表达：支持 `明早`、`明晚`、`今晚` 和更宽松的 `开...` 创建意图。
- 新增 deterministic validator：`commandValidator.ts` 统一校验时间范围、提醒范围、候选是否属于当前日历、重复候选和缺失修改时间。
- 本地规则解析和 DeepSeek 兜底结果进入确认卡片前都已接入 validator。
- 新增 validator 单元测试，覆盖非法时间、非法提醒、外部候选、重复候选和缺失修改时间。
- 后端新增 `response_validator.py`，对 DeepSeek JSON 输出做语义校验：action 类型一致性、写操作目标、时间范围、提醒范围和低置信度降级。
- DeepSeek client 已在 Pydantic 结构校验后调用后端 response validator，非法 LLM 输出会降级为 `unknown` 追问。
- 新增后端 unittest，覆盖缺少开始时间、非法时间范围、删除缺目标、修改缺新值、action 类型不一致、非法提醒和低置信度。
- 新增 `/api/parse-command` 接口级测试，使用 FastAPI TestClient mock DeepSeek 返回，覆盖合法 create、非法语义降级、非 JSON、非法 schema、上游 500 和未配置 key。
- 新增 `agentMemory.ts`，定义短期 Memory：lastIntent、lastMentionedEvents、lastCandidates、pendingClarification、selectedEventId、conversationSummary。
- Agent 状态机接入 Memory，保留 `lastReferencedEvent` 兼容字段，同时支持记忆最近提到的日程和候选列表。
- App 接入 Memory 引用解析，支持“提醒也提前十五分钟”“把地点改成 A 楼”“刚才那个取消”。
- 支持“不是这个，是第二个”类候选纠正，更新 Memory 选中项。
- 新增 10 条 Memory 单元测试，覆盖初始化、意图记忆、候选记忆、选择候选、引用解析、候选序号解析和纠正检测。

## 正在做

- 阶段 10 短期 Memory 初版已接入，下一步继续扩展多轮修改场景和候选纠正体验。

## 阻塞项

- 暂无。

## 下一步

1. 扩展多轮 Memory：支持“不是这个，是第二个”后直接继续确认/删除/修改 pending action。
2. 支持更多省略修改字段：备注、时间偏移、提醒叠加。
3. 复跑浏览器完整演示路径，并决定是否提交阶段 9+阶段 10 初版 commit。

## 不做清单

第一阶段不做：

- 登录系统。
- 第三方日历同步。
- 独立后台管理系统。

## 验证记录

- `npm install` 生成依赖和 `package-lock.json`，命令等待超时但依赖已可用。
- `npm run build` 通过。
- `http://127.0.0.1:5173` 返回 200。
- Browser 插件可调用工具未暴露，已用构建和本地 HTTP 检查替代本轮验证。
- 本地日历数据层接入后，`npm run build` 再次通过。
- 数据层接入后，`http://127.0.0.1:5173` 返回 200。
- 文字指令 Agent 接入后，`npm run build` 通过。
- `npm run test` 通过，1 个测试文件、3 个测试用例。
- 语音识别与播报接入后，`npm run build` 通过。
- 语音识别与播报接入后，`npm run test` 通过。
- 撤销、冲突检测和导出功能接入后，`npm run build` 通过。
- 撤销、冲突检测和导出功能接入后，`npm run test` 通过。
- 2026-05-31 发现删除候选匹配测试受系统日期影响失败，已修复解析器时间基准。
- 修复后 `npm run test -- --run` 通过，1 个测试文件、3 个测试用例。
- 修复后 `npm run build` 通过。
- 空闲时间查询接入后 `npm run test -- --run` 通过，1 个测试文件、4 个测试用例。
- 空闲时间查询接入后 `npm run build` 通过。
- 前端视觉优化后 `npm run test -- --run` 通过，1 个测试文件、4 个测试用例。
- 前端视觉优化后 `npm run build` 通过。
- 已用 Chrome headless 生成桌面和移动端截图，修复移动端输入区横向溢出。
- DeepSeek 后端代理接入后 `npm run build` 通过。
- `conda run -n base python -m py_compile backend\app\*.py` 通过。
- `conda run -n base python -c "from backend.app.main import app"` 通过。
- 后端健康检查返回 `deepseekConfigured: true`。
- 后端健康检查返回 ASR 当前配置：`base / cpu / int8 / maxConcurrent=1`。
- 前端本地 ASR、多候选和上下文指代接入后 `npm run build` 通过。
- 前端本地 ASR、多候选和上下文指代接入后 `npm run test` 通过，1 个测试文件、4 个测试用例。
- 本地规则增强后 `npm run test` 通过，1 个测试文件、8 个测试用例。
- 本地规则增强后 `npm run build` 通过。
- DeepSeek 兜底实测复杂时间段指令返回 `create_event`，包含 `10:15` 到 `11:30` 和 15 分钟提醒。
- 月历视图接入后 `npm run build` 通过。
- 月历视图接入后 `npm run test` 通过，1 个测试文件、8 个测试用例。
- Agent 状态机基础抽取后 `npm run build` 通过。
- Agent 状态机基础抽取后 `npm run test` 通过，2 个测试文件、11 个测试用例。
- Agent pending action factory 抽取后 `npm run build` 通过。
- Agent pending action factory 抽取后 `npm run test` 通过，2 个测试文件、12 个测试用例。
- Agent 确认执行结果抽取后 `npm run build` 通过。
- Agent 确认执行结果抽取后 `npm run test` 通过，2 个测试文件、13 个测试用例。
- 解析器评测集初版接入后 `npm run test` 通过，3 个测试文件、22 个测试用例。
- 解析器评测集初版接入后 `npm run build` 通过。
- 2026-05-31 浏览器端演示回归首次发现无日期修改会误落到今天，已修复并补测试。
- 修复后 `npm run test` 通过，3 个测试文件、24 个测试用例。
- 修复后 `npm run build` 通过。
- 解析器评测集扩展后发现 `下周日` 被解析为下一个周日，已修复为下一周周日。
- 扩展后 `npm run test` 通过，3 个测试文件、33 个测试用例。
- 扩展后 `npm run build` 通过。
- Chrome headless DevTools 驱动真实页面回归通过：新增、查询、修改、冲突、取消、删除、撤销和语音模式显示均通过。
- 解析器评测集扩展到 55 条后 `npm run test` 通过，3 个测试文件、70 个测试用例。
- `npm run eval:parser` 通过，输出：55 cases，intent accuracy 100%，slot accuracy 100%，fallback rate 100%，unsafe action rate 0%。
- validator 接入后 `npm run test` 通过，4 个测试文件、77 个测试用例。
- validator 接入后 `npm run eval:parser` 通过，55 cases，intent accuracy 100%，slot accuracy 100%，fallback rate 100%，unsafe action rate 0%。
- validator 接入后 `npm run build` 通过。
- validator 接入后 Chrome headless DevTools 浏览器回归通过：新增、查询、修改、冲突、取消、删除、撤销和语音模式显示均通过。
- 后端 response validator 接入后 `conda run -n base python -m py_compile backend\app\asr.py backend\app\config.py backend\app\deepseek_client.py backend\app\main.py backend\app\schemas.py backend\app\response_validator.py backend\tests\test_response_validator.py` 通过。
- 后端 response validator 接入后 `conda run -n base python -m unittest discover -s backend\tests -p "test_*.py"` 通过，8 个测试。
- `/api/parse-command` 接口级回归接入后 `conda run -n base python -m py_compile backend\app\asr.py backend\app\config.py backend\app\deepseek_client.py backend\app\main.py backend\app\schemas.py backend\app\response_validator.py backend\tests\test_response_validator.py backend\tests\test_parse_command_api.py` 通过。
- `/api/parse-command` 接口级回归接入后 `conda run -n base python -m unittest discover -s backend\tests -p "test_*.py"` 通过，14 个测试。
- 接口级回归接入后 `npm run test` 通过，4 个测试文件、77 个测试用例。
- 接口级回归接入后 `npm run eval:parser` 通过，55 cases，intent accuracy 100%，slot accuracy 100%，fallback rate 100%，unsafe action rate 0%。
- 接口级回归接入后 `npm run build` 通过。
- 接口级回归接入后 Chrome headless DevTools 浏览器回归通过：新增、查询、修改、冲突、取消、删除、撤销和语音模式显示均通过。
- Memory 初版接入后 `npm run test` 通过，5 个测试文件、90 个测试用例。
- Memory 初版接入后 `npm run build` 通过。
- Memory 初版接入后 Chrome headless DevTools 多轮回归通过：创建后直接说“提醒也提前十五分钟”“把地点改成 A 楼”“刚才那个取消”均成功。
